> Add history for send messages
> Find a way to make multiword messages better so you don't need ' "" '
 - Could mabey make a text editor like nano?
> Add a way to test the connection of other machines without sending messages to them
> Add a backdoor into machines.... XD
